import React, { FC } from "react";
import { BrowserRouter } from "react-router-dom"

const App:FC = () => {
  return(
    <BrowserRouter>
      WORKING
    </BrowserRouter>
  )
};

export default App;

